---
import { SITE } from '../data/content';

interface Props {
  class?: string;
}

const { class: className = 'btn btn--primary' } = Astro.props;
---

<a href={SITE.appWebUrl} class={className}>
  <slot />
</a>
